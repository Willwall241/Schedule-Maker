import React from 'react';
import './style.css';
import Button from '@material-ui/core/Button';

const Home = props => {
  return (
    <div>
      <Button href='/'>
        Login
      </Button>
    </div>
  );
};

export default Home;
